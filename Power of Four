class Solution:
    def isPowerOfFour(self, n: int) -> bool:
        if n <= 0:
            return False
        else:
            x = 1
            while x < n:
                x *= 4
        
            return x == n
